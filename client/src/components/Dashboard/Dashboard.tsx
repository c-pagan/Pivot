import { Box, Grid } from "@mui/material";
import { useEffect } from "react";

function Dashboard() {
  const pageContent =
    <Box>
      <Grid container>
        <Grid item xs={12}>
          Placeholder
        </Grid>
      </Grid>
    </Box>

  return (
    pageContent
  );
};

export default Dashboard;
